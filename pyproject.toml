[tool.uv.workspace]
members = [
    "pyama",
    "pyama-gui",
]

[tool.uv.sources]
pyama = { workspace = true }
pyama-gui = { workspace = true }

[dependency-groups]
dev = ["ty", "ruff", "black", "ipykernel", "pytest"]
